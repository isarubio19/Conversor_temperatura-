# Conversor_temperatura-
Programa em C que converte temperaturas de Fahrenheit para Celsius em loop contínuo. Utiliza uma função separada converterParaCelsius() que aplica a fórmula C = 5/9 * (F - 32), retornando o resultado com duas casas decimais.
